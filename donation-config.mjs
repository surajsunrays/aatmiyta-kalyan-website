// Keep disabled until the NGO's bank-verified UPI details are supplied.
// No static QR replacement is needed: a fresh QR includes each donor's amount.
export const donationConfig = Object.freeze({
  enabled: false,
  upiId: '',
  payeeName: 'Aatmiyata Kalyan',
});
